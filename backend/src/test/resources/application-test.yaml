server:
  port: 8080
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      username: mtb
      password: mtbpassword
      database: mtb
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: sso